═══════════════════════════════════════════════════════════════
  DATABASE MIGRATION SUMMARY
═══════════════════════════════════════════════════════════════

🔍 MIGRATION STATUS: BLOCKED BY CONSTRAINT

Current Database State:
├── Total Users: 16
├── admin: 2
├── pembina: 13  ← TO BE MIGRATED
└── siswa: 1

Problem Identified:
─────────────────
The check constraint "user_role_check" on users table only allows:
  - admin
  - pembina
  - siswa

It does NOT include 'coach', so UPDATE attempts fail with:
"new row for relation 'users' violates check constraint 'user_role_check'"

Solution:
─────────
Execute SQL in Supabase Dashboard → SQL Editor in this order:

1️⃣  ALTER TABLE public.users DROP CONSTRAINT IF EXISTS user_role_check;

2️⃣  ALTER TABLE public.users ADD CONSTRAINT user_role_check 
    CHECK (role IN ('admin', 'coach', 'siswa'));

3️⃣  UPDATE public.users SET role = 'coach' WHERE role = 'pembina';

4️⃣  SELECT role, COUNT(*) as count FROM public.users 
    GROUP BY role ORDER BY role;

Expected Result After Migration:
─────────────────────────────────
admin: 2
coach: 13  ← Successfully migrated from pembina
siswa: 1

Files Created:
├── migrate_via_rest.js (REST API approach - failed)
├── migrate_v2.js (Alternative attempt - failed)
├── migrate_final.js (Identified constraint issue)
└── migration_steps.md (Manual instructions)

Next Step:
──────────
Go to: https://rclqnjgfoqgsouxcfcjz.supabase.co/project/default/sql/new
Copy-paste the 4 SQL statements from step 1️⃣ above and execute.

═══════════════════════════════════════════════════════════════
